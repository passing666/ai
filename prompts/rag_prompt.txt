Instructions: Use the provided documents to answer the question concisely.
- Provide a short answer (1-3 sentences) in Chinese.
- For any factual claim, include citations to the provided documents using the bracketed index format [1], [2], ...
- At the end, return a JSON object with keys: "answer" (string) and "sources" (an array of objects with keys "id" and "title").

Question: {q}

Documents:
{docs}

Answer and citations (JSON):
{{"answer": "...", "sources": [{{"id": "...", "title": "..."}}]}}

Notes:
- Truncate long documents to the most relevant 800 characters per document before including.
- Prefer direct quotes from documents when supporting factual claims.
- If no document supports an assertion, say "无法从提供的文档中得出结论。" and do not invent facts.

你是一个助手，请使用下面的文档内容回答用户的问题。

用户问题：{q}

以下文档可用：
{docs}

请用简洁的中文回答，最多 3 段，每段不超过 3 行。回答后列出 1-3 个简短引用，格式为：`[编号] 标题 - 链接（若有）`。

如果文档中信息不足，请说明并给出可行的检索建议。
